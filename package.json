{
  "main": "src/index.ts",
  "devDependencies": {
    "@types/jest": "^25.1.4",
    "jest": "^25.1.0",
    "mockdate": "^2.0.5",
    "moment": "^2.24.0",
    "ts-jest": "^25.2.1",
    "typescript": "^3.8.3"
  },
  "scripts": {
    "test": "jest",
    "launch": "ts-node-dev --inspect=0.0.0.0:9234 . --transpile-only --ignore-watch node_modules --watch ./src",
    "dockerUp": "docker-compose up --build servertest"
  },
  "dependencies": {
    "express": "^4.17.1",
    "ts-node": "^8.8.2",
    "ts-node-dev": "^1.0.0-pre.44"
  }
}
